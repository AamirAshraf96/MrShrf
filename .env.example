# Formspree form endpoint (inlined at build time — also add in Cloudflare Pages → Environment variables for production builds)
VITE_FORMSPREE_ENDPOINT=https://formspree.io/f/xgopgdqa

# Optional — local testing of `/api/contact` only (Wrangler + Resend):
# VITE_CONTACT_API_ORIGIN=http://127.0.0.1:8788
# Server secret for that path: RESEND_API_KEY (Cloudflare env or .dev.vars locally)
